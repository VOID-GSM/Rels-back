name: CD

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew test --no-daemon

  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Execute Remote Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GSMSV_HOST }}
          username: ${{ secrets.GSMSV_USERNAME }}
          password: ${{ secrets.GSMSV_PASSWORD }}
          port: ${{ secrets.GSMSV_PORT }}
          script: |
            cd ~/Rels-back || exit 1
            
            git pull origin main
            chmod +x gradlew
            ./gradlew build -x test
            
            PID=$(pgrep -f 'Rels-.*\.jar')
            if [ -n "$PID" ]; then
              kill -15 $PID
              sleep 3
            fi
            
            # setsid를 사용하여 SSH 세션과 완전히 독립된 신규 세션에서 실행
            setsid nohup java -jar build/libs/*-SNAPSHOT.jar > app.log 2>&1 < /dev/null &
            sleep 2